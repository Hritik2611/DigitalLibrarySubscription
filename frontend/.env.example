# Leave empty for local development (Vite proxies /api to localhost:5000).
# For deployment, set this to the public Render backend URL, for example:
# VITE_API_URL=https://your-library-api.onrender.com
VITE_API_URL=
